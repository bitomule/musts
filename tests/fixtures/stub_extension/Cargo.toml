[package]
name = "stub-extension"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Configurable test stub for the musts ⇄ extension protocol. Behaviour driven by MUSTS_STUB_* env vars."

[[bin]]
name = "stub-extension"
path = "src/main.rs"

[dependencies]
musts-protocol = { path = "../../../crates/musts-protocol" }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
